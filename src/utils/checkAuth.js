const checkAuth = (pathName, auth) => {
   // console.log('----', pathName);
   // console.log('----', auth);
   
   // if (pathName === '/login') location.pathname='myData';
   // login 和 pathname 没有必要做权限
};
export default checkAuth;
  